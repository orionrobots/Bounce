language: node_js
addons:
  apt: 
    packages:
      - g++-4.8
      - g++-4.8-multilib
      - gcc-multilib
before_script:
  - chmod +x update_libs.sh
script:
  - ./update_libs.sh
  - cd Bounce; npm install
  - cd Bounce; npm run-script package
after_script:
  - ls -l
